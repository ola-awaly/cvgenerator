import { CvItemListDirective } from './cv-item-list.directive';

describe('CvItemListDirective', () => {
  it('should create an instance', () => {
    const directive = new CvItemListDirective();
    expect(directive).toBeTruthy();
  });
});
